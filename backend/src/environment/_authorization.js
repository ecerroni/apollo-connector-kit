import APP from '$/settings/app.json';

export const { CONSTANTS: { UNAUTHORIZED = 'Unauthorized!' } = {} } = APP;
export const { CONSTANTS: { FORBIDDEN = 'Forbidden' } = {} } = APP;
export const { CONSTANTS: { NOT_ALLOWED = 'Not allowed' } = {} } = APP;
